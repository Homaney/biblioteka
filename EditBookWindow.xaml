<Window x:Class="biblioteka.EditBookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактировать книгу"
        Height="777" Width="420"
        WindowStartupLocation="CenterScreen"
        Background="#121212"
        FontFamily="Segoe UI, Inter">
    <Window.Resources>
        <!-- 🎨 Цвета -->
        <SolidColorBrush x:Key="Primary" Color="#6C5CE7"/>
        <SolidColorBrush x:Key="Success" Color="#00B894"/>
        <SolidColorBrush x:Key="CardBg" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#B0B0B0"/>
        <SolidColorBrush x:Key="Border" Color="#333333"/>
        <SolidColorBrush x:Key="InputBg" Color="#2A2A2A"/>
        <SolidColorBrush x:Key="ChipBg" Color="#3A3A3A"/>
        <SolidColorBrush x:Key="ChipText" Color="#FFFFFF"/>

        <!-- 🧩 Стиль для TextBox -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputBg}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="#6C5CE7"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <Grid>
                                <TextBlock x:Name="Placeholder"
                                           Text="{TemplateBinding Tag}"
                                           Foreground="#666"
                                           FontStyle="Italic"
                                           Margin="14,10"
                                           IsHitTestVisible="False"
                                           Opacity="0.7"
                                           Visibility="Collapsed"/>
                                <ScrollViewer x:Name="PART_ContentHost" Margin="1"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Text" Value="{x:Null}">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource Primary}"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#6C5CE7" BlurRadius="12" ShadowDepth="0" Opacity="0.5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 🎚 Современный ComboBox -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource InputBg}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock x:Name="Placeholder"
                                               Grid.Column="0"
                                               Text="{TemplateBinding Tag}"
                                               Foreground="#666"
                                               FontStyle="Italic"
                                               Margin="14,0"
                                               VerticalAlignment="Center"
                                               IsHitTestVisible="False"
                                               Opacity="0.7"
                                               Visibility="Collapsed"/>

                                    <TextBox x:Name="PART_EditableTextBox"
                                             Grid.Column="0"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Margin="14,0"
                                             VerticalAlignment="Center"
                                             Foreground="{TemplateBinding Foreground}"
                                             FontSize="{TemplateBinding FontSize}"
                                             IsReadOnly="{TemplateBinding IsReadOnly}"
                                             Focusable="True"
                                             Panel.ZIndex="1"/>

                                    <ToggleButton x:Name="ToggleButton"
                                                  Grid.Column="1"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  Focusable="False"
                                                  Margin="0,0,14,0"
                                                  Width="20" Height="20">
                                        <Path Fill="#AAA" Data="M 0 0 L 4 4 L 8 0 Z"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                    </ToggleButton>
                                </Grid>
                            </Border>

                            <Popup x:Name="Popup"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False">
                                <Border Background="#2A2A2A"
                                        BorderBrush="#444"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="200">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value=""/>
                                    <Condition Binding="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" Value="False"/>
                                </MultiDataTrigger.Conditions>
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </MultiDataTrigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource Primary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ➕ Кнопка -->
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5A4FCF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 💬 Улучшенный шаблон "чипа" автора -->
        <DataTemplate x:Key="AuthorChipTemplate">
            <Border Background="{StaticResource ChipBg}" 
                    CornerRadius="20" 
                    Margin="4,4,0,0" 
                    Padding="10,6"
                    VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" >
                    <TextBlock Text="{Binding}" 
                               Foreground="{StaticResource ChipText}" 
                               FontSize="13"
                               TextTrimming="CharacterEllipsis"
                               MaxWidth="180"
                               VerticalAlignment="Center"/>

                    <Button Content="×"
                            Width="22" Height="22"
                            FontSize="13"
                            FontWeight="Bold"
                            Margin="6,0,0,0"
                            Padding="0"
                            Background="Transparent"
                            Foreground="#FF6B6B"
                            BorderThickness="0"
                            Cursor="Hand"
                            Tag="{Binding}"
                            Click="RemoveAuthor_Click">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Opacity" Value="0.7"/>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="1"/>
                                        <Setter Property="Foreground" Value="#FF4C4C"/>
                                        <Setter Property="RenderTransform">
                                            <Setter.Value>
                                                <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <!-- 🪶 Основная разметка -->
    <Border Margin="20" Background="{StaticResource CardBg}" CornerRadius="16"
            BorderBrush="{StaticResource Border}" BorderThickness="1">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="25">
                <TextBlock Text="Редактирование книги"
                           FontSize="24" FontWeight="Bold"
                           Foreground="{StaticResource Primary}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,16"/>

                <TextBlock x:Name="IdentifierLabel" Text="ID:"
                           FontWeight="Bold" Foreground="#74B9FF"
                           FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="Название книги" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,4"/>
                <TextBox x:Name="TitleBox" Style="{StaticResource ModernTextBox}" Tag="Введите название..." Margin="0,0,0,12"/>

                <TextBlock Text="Авторы" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,4"/>
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox x:Name="AuthorBox"
                              Grid.Column="0"
                              Style="{StaticResource ModernComboBox}"
                              IsEditable="True"
                              Tag="Введите автора"/>
                    <Button Grid.Column="1" Content="+" Style="{StaticResource IconButton}" Click="AddAuthor_Click"/>
                </Grid>

                <ItemsControl x:Name="SelectedAuthorsList"
                              ItemTemplate="{StaticResource AuthorChipTemplate}"
                              Margin="0,0,0,12">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>

                <TextBlock Text="Раздел" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,4"/>
                <TextBox x:Name="RazdelBox" Style="{StaticResource ModernTextBox}" Tag="Фантастика, детектив..." Margin="0,0,0,12"/>

                <TextBlock Text="Год издания" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,4"/>
                <TextBox x:Name="YearBox" Style="{StaticResource ModernTextBox}" Tag="2023" Margin="0,0,0,12"/>

                <TextBlock Text="Количество экземпляров" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,4"/>
                <TextBox x:Name="QuantityBox" Style="{StaticResource ModernTextBox}" Tag="5" Margin="0,0,0,12"/>

                <TextBlock Text="Описание" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,4"/>
                <TextBox x:Name="DescriptionBox"
                         Style="{StaticResource ModernTextBox}"
                         Height="100" AcceptsReturn="True"
                         TextWrapping="Wrap"
                         Tag="Краткое описание книги..."
                         Margin="0,0,0,20"/>

                <Button Content="Сохранить изменения"
                        Height="48" Width="220"
                        FontSize="16" FontWeight="SemiBold"
                        Background="{StaticResource Success}" Foreground="White"
                        Cursor="Hand" HorizontalAlignment="Center"
                        Click="SaveChanges_Click"/>
            </StackPanel>
        </ScrollViewer>
    </Border>
</Window>
