<Window x:Class="biblioteka.EditBookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:biblioteka"
        Title="Редактирование книги"
        Height="810" Width="620"
        WindowStartupLocation="CenterOwner"
        Background="#0F0F0F"
        FontFamily="Segoe UI"
        ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryPurple" Color="#8B5CF6"/>
        <SolidColorBrush x:Key="PrimaryPurpleDark" Color="#7C3AED"/>
        <SolidColorBrush x:Key="Success" Color="#10B981"/>
        <SolidColorBrush x:Key="SuccessDark" Color="#059669"/>
        <SolidColorBrush x:Key="Danger" Color="#EF4444"/>
        <SolidColorBrush x:Key="DangerDark" Color="#DC2626"/>
        <SolidColorBrush x:Key="Info" Color="#3B82F6"/>
        <SolidColorBrush x:Key="InfoDark" Color="#2563EB"/>
        <SolidColorBrush x:Key="Warning" Color="#F59E0B"/>
        <SolidColorBrush x:Key="Background" Color="#0F0F0F"/>
        <SolidColorBrush x:Key="CardBackground" Color="#1A1A1A"/>
        <SolidColorBrush x:Key="CardBorder" Color="#2D2D2D"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#A1A1AA"/>
        <SolidColorBrush x:Key="TextTertiary" Color="#71717A"/>

        <local:BoolToYesNoConverter x:Key="BoolToYesNoConverter"/>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="CaretBrush" Value="{StaticResource PrimaryPurple}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="8,0"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryPurple}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComboBoxLikeTextBox" TargetType="TextBox" BasedOn="{StaticResource ModernTextBox}">
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="8,4,24,4"/>
        </Style>

        <Style x:Key="ClickableBorder" TargetType="Border">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryPurple}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryPurpleDark}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{StaticResource Success}"/>
            <Setter Property="Padding" Value="10,4"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SuccessDark}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{StaticResource Danger}"/>
            <Setter Property="Padding" Value="10,4"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource DangerDark}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TagButton" TargetType="Button">
            <Setter Property="Background" Value="#374151"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="14"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0"
                                                  Content="{TemplateBinding Content}"
                                                  VerticalAlignment="Center"
                                                  Margin="4,0,2,0"
                                                  TextElement.FontSize="10"/>
                                <TextBlock Grid.Column="1"
                                           Text="×" 
                                           FontWeight="Bold"
                                           FontSize="11"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource Danger}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="8" Background="{StaticResource CardBackground}" CornerRadius="8" BorderBrush="{StaticResource CardBorder}" BorderThickness="1">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="12" MaxWidth="560">
                <TextBlock Text="✏️ Редактирование книги" 
                          FontSize="20" 
                          FontWeight="Bold"
                          Foreground="{StaticResource TextPrimary}" 
                          HorizontalAlignment="Center" 
                          Margin="0,0,0,8"/>

                <TextBlock x:Name="IdentifierLabel" 
                          Text="ID: " 
                          FontSize="13" 
                          FontWeight="SemiBold"
                          Foreground="{StaticResource PrimaryPurple}" 
                          HorizontalAlignment="Center" 
                          Margin="0,0,0,8"/>

                <TextBlock Text="Название книги" 
                          Foreground="{StaticResource TextSecondary}" 
                          FontSize="11" 
                          FontWeight="SemiBold"
                          Margin="0,0,0,2"/>
                <TextBox x:Name="TitleBox" 
                        Style="{StaticResource ModernTextBox}"
                        Height="32"
                        GotFocus="TextBox_GotFocus"
                        Margin="0,0,0,4"/>

                <TextBlock Text="Авторы" 
                          Foreground="{StaticResource TextSecondary}" 
                          FontSize="11" 
                          FontWeight="SemiBold"
                          Margin="0,4,0,2"/>
                <Grid Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" 
                            Style="{StaticResource ClickableBorder}"
                            MouseDown="AuthorsTextBox_MouseDown">
                        <Grid>
                            <TextBox x:Name="AuthorsTextBox"
                                     Style="{StaticResource ComboBoxLikeTextBox}"
                                     Height="32"
                                     Tag="Выберите автора..."
                                     IsHitTestVisible="False"
                                     GotFocus="TextBox_GotFocus"/>
                            <Path Data="M0,0 L4,4 8,0 z"
                                  Fill="{StaticResource TextSecondary}"
                                  HorizontalAlignment="Right"
                                  VerticalAlignment="Center"
                                  Margin="0,0,8,0"
                                  Stretch="Uniform"
                                  Width="8"
                                  Height="5"
                                  IsHitTestVisible="False"/>
                        </Grid>
                    </Border>
                    <Button Content="➕ Добавить" 
                            Grid.Column="1" 
                            Margin="5,0,0,0"
                            Style="{StaticResource PrimaryButton}"
                            Click="AddAuthor_Click"
                            Padding="8,4"
                            Height="32"/>
                </Grid>

                <TextBlock Text="Выбранные авторы:" 
                          Foreground="{StaticResource TextSecondary}" 
                          FontSize="10" 
                          Margin="0,4,0,2"/>
                <Border Background="{StaticResource CardBackground}" 
                        BorderBrush="{StaticResource CardBorder}" 
                        BorderThickness="1" 
                        CornerRadius="6" 
                        MinHeight="36" MaxHeight="70"
                        Margin="0,0,0,6" 
                        Padding="4">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="SelectedAuthorsList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding FullName}" 
                                            Style="{StaticResource TagButton}"
                                            Click="RemoveAuthor_Click"
                                            Tag="{Binding}"
                                            Margin="0,1,2,1"
                                            Height="20"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>

                <TextBlock Text="УДК" 
                          Foreground="{StaticResource TextSecondary}" 
                          FontSize="11" 
                          FontWeight="SemiBold"
                          Margin="0,0,0,2"/>
                <Border Style="{StaticResource ClickableBorder}" 
                        MouseDown="UDKTextBox_MouseDown">
                    <Grid>
                        <TextBox x:Name="UDKTextBox"
                                 Style="{StaticResource ComboBoxLikeTextBox}"
                                 Height="32"
                                 Tag="Выберите УДК..."
                                 IsHitTestVisible="False"
                                 GotFocus="TextBox_GotFocus"/>
                        <Path Data="M0,0 L4,4 8,0 z"
                              Fill="{StaticResource TextSecondary}"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Center"
                              Margin="0,0,8,0"
                              Stretch="Uniform"
                              Width="8"
                              Height="5"
                              IsHitTestVisible="False"/>
                    </Grid>
                </Border>

                <TextBlock Text="Год издания" 
                          Foreground="{StaticResource TextSecondary}" 
                          FontSize="11" 
                          FontWeight="SemiBold"
                          Margin="0,6,0,2"/>
                <TextBox x:Name="YearBox" 
                        Style="{StaticResource ModernTextBox}"
                        Height="32"
                        Margin="0,0,0,4"
                        GotFocus="TextBox_GotFocus"/>

                <TextBlock Text="Описание" 
                          Foreground="{StaticResource TextSecondary}" 
                          FontSize="11" 
                          FontWeight="SemiBold"
                          Margin="0,0,0,2"/>
                <TextBox x:Name="DescriptionBox" 
                        Style="{StaticResource ModernTextBox}"
                        Height="50" 
                        AcceptsReturn="True" 
                        TextWrapping="Wrap"
                        VerticalScrollBarVisibility="Auto"
                        Margin="0,0,0,6"
                        GotFocus="TextBox_GotFocus"
                        VerticalContentAlignment="Top"
                        Padding="8,3"/>

                <!-- Список экземпляров -->
                <TextBlock Text="📚 Экземпляры книги" 
                          Foreground="{StaticResource PrimaryPurple}" 
                          FontSize="14" 
                          FontWeight="Bold"
                          Margin="0,10,0,5"/>
                <Border Background="{StaticResource CardBackground}" 
                        BorderBrush="{StaticResource CardBorder}" 
                        BorderThickness="1" 
                        CornerRadius="6" 
                        Padding="8" 
                        Margin="0,0,0,10">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="200">
                        <ItemsControl x:Name="InstancesItemsControl">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#252525" CornerRadius="4" Padding="8,6" Margin="0,0,0,4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding InventoryNumber}" Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <TextBlock Text="Статус: " Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                                                    <TextBlock Text="{Binding Status}" Foreground="{StaticResource TextPrimary}" FontSize="11"/>
                                                    <TextBlock Text=" • Цена: " Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="10,0,0,0"/>
                                                    <TextBlock Text="{Binding Price, StringFormat={}{0:F2} BYN}" Foreground="{StaticResource Success}" FontSize="11" FontWeight="SemiBold"/>
                                                    <TextBlock Text=" • Продажа: " Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="10,0,0,0"/>
                                                    <TextBlock Text="{Binding CanBeSold, Converter={StaticResource BoolToYesNoConverter}}" Foreground="{StaticResource Info}" FontSize="11"/>
                                                </StackPanel>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                                <Button Content="✏️" Width="28" Height="28" 
                                                        Style="{StaticResource PrimaryButton}"
                                                        Click="EditInstance_Click" Tag="{Binding}"
                                                        Margin="0,0,5,0" Padding="0"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="InstancesStatsText" Text="" Foreground="{StaticResource TextSecondary}" FontSize="11" VerticalAlignment="Center"/>
                    <Button Content="➕ Добавить экземпляр" 
                            Style="{StaticResource SuccessButton}"
                            Grid.Column="1"
                            Click="AddInstance_Click"
                            Padding="10,6"
                            Height="32"/>
                </Grid>

                <Button Content="💾 Сохранить изменения" 
                        Style="{StaticResource SuccessButton}"
                        Click="SaveChanges_Click"
                        Height="36"
                        FontSize="13"
                        Padding="16,6"
                        Margin="0,0,0,8"/>
            </StackPanel>
        </ScrollViewer>
    </Border>
</Window>