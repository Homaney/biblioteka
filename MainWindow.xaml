<Window x:Class="biblioteka.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Научно-техническая библиотека"
        Height="500" Width="850"
        WindowStartupLocation="CenterScreen"
        Background="#f5f5f5">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Каталог книг" 
                   FontSize="22" 
                   FontWeight="Bold" 
                   Foreground="#333" 
                   Margin="0 0 0 10"/>

        <!-- Панель кнопок -->
        <StackPanel Orientation="Horizontal" 
                    Grid.Row="1"
                    HorizontalAlignment="Center"
                    Margin="0 0 0 10">
            <Button Content="Добавить книгу" Click="AddBook_Click"
                    Width="150" Height="40" Margin="5"
                    Background="#4CAF50" Foreground="White" FontWeight="Bold"
                    BorderThickness="0" Cursor="Hand"/>
            <Button Content="Удалить книгу" Click="DeleteBook_Click"
                    Width="150" Height="40" Margin="5"
                    Background="#E53935" Foreground="White" FontWeight="Bold"
                    BorderThickness="0" Cursor="Hand"/>
            <Button Content="Выдать книгу" Click="IssueBook_Click"
                    Width="150" Height="40" Margin="5"
                    Background="#2196F3" Foreground="White" FontWeight="Bold"
                    BorderThickness="0" Cursor="Hand"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="BooksDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  AlternatingRowBackground="#F0F0F0"
                  RowBackground="White"
                  GridLinesVisibility="None"
                  HeadersVisibility="Column"
                  FontSize="14"
                  Foreground="#333"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  IsReadOnly="True"
                  BorderBrush="#DDD"
                  BorderThickness="1"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Название книги" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Автор" Binding="{Binding Author}" Width="150"/>
                <DataGridTextColumn Header="Год издания" Binding="{Binding Yearr}" Width="100"/>
                <DataGridTextColumn Header="Жанр" Binding="{Binding Genre}" Width="120"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
