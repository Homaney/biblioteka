<Window x:Class="biblioteka.EditUDKWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="✏️ Редактирование УДК"
        Height="444" Width="500"
        WindowStartupLocation="CenterOwner"
        Background="#0F0F0F"
        FontFamily="Segoe UI"
        ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryPurple" Color="#8B5CF6"/>
        <SolidColorBrush x:Key="PrimaryPurpleDark" Color="#7C3AED"/>
        <SolidColorBrush x:Key="Info" Color="#3B82F6"/>
        <SolidColorBrush x:Key="InfoDark" Color="#2563EB"/>
        <SolidColorBrush x:Key="CardBackground" Color="#1A1A1A"/>
        <SolidColorBrush x:Key="CardBorder" Color="#2D2D2D"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#A1A1AA"/>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="{StaticResource PrimaryPurple}"/>
        </Style>

        <Style x:Key="InfoButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Info}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource InfoDark}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButton" TargetType="Button" BasedOn="{StaticResource InfoButton}">
            <Setter Property="Background" Value="#6B7280"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4B5563"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Margin="20,20,20,20" Background="{StaticResource CardBackground}" CornerRadius="8" BorderBrush="{StaticResource CardBorder}" BorderThickness="1">
        <StackPanel Margin="24">
            <TextBlock Text="✏️ Редактирование УДК" 
                      FontSize="20" 
                      FontWeight="Bold"
                      Foreground="{StaticResource TextPrimary}" 
                      HorizontalAlignment="Center" 
                      Margin="0,0,0,20"/>

            <TextBlock Text="Код УДК" 
                      Foreground="{StaticResource TextSecondary}" 
                      FontSize="13" 
                      FontWeight="SemiBold"
                      Margin="0,0,0,6"/>
            <TextBox x:Name="CodeTextBox" 
                    Style="{StaticResource ModernTextBox}"
                    Height="40"
                    Margin="0,0,0,16"/>

            <TextBlock Text="Описание" 
                      Foreground="{StaticResource TextSecondary}" 
                      FontSize="13" 
                      FontWeight="SemiBold"
                      Margin="0,0,0,6"/>
            <TextBox x:Name="DescriptionTextBox" 
                    Style="{StaticResource ModernTextBox}"
                    Height="80" 
                    AcceptsReturn="True" 
                    TextWrapping="Wrap"
                    VerticalScrollBarVisibility="Auto"
                    Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="💾 Сохранить" 
                        Style="{StaticResource InfoButton}"
                        Click="SaveButton_Click"
                        Width="120"
                        Margin="0,0,12,0"/>
                <Button Content="❌ Отмена" 
                        Style="{StaticResource CancelButton}"
                        Click="CancelButton_Click"
                        Width="120"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>