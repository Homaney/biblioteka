<Window x:Class="biblioteka.EditInstanceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактировать экземпляр"
        Height="350" Width="390"
        WindowStartupLocation="CenterOwner"
        Background="#0F0F0F"
        FontFamily="Segoe UI"
        ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryPurple" Color="#8B5CF6"/>
        <SolidColorBrush x:Key="Success" Color="#10B981"/>
        <SolidColorBrush x:Key="CardBackground" Color="#1A1A1A"/>
        <SolidColorBrush x:Key="CardBorder" Color="#2D2D2D"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#A1A1AA"/>
        <SolidColorBrush x:Key="InputBackground" Color="#2A2A2A"/>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="CaretBrush" Value="{StaticResource PrimaryPurple}"/>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Success}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="40"/>
        </Style>

        <Style x:Key="CheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
    </Window.Resources>

    <Border Margin="15" Background="{StaticResource CardBackground}" CornerRadius="8" BorderBrush="{StaticResource CardBorder}" BorderThickness="1">
        <StackPanel Margin="20">
            <TextBlock Text="✏️ Редактирование экземпляра" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,20"/>

            <TextBlock Text="Инвентарный номер:" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,4"/>
            <TextBlock x:Name="InventoryNumberText" Text="" Foreground="{StaticResource TextPrimary}" FontSize="14" Margin="0,0,0,15" FontWeight="SemiBold"/>

            <TextBlock Text="Цена (BYN):" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,4"/>
            <TextBox x:Name="PriceBox" Text="0,00" Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>

            <CheckBox x:Name="CanBeSoldCheckBox" Content="Можно продавать" Style="{StaticResource CheckBoxStyle}"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="💾 Сохранить" Style="{StaticResource SuccessButton}" Width="120" Click="SaveButton_Click" Margin="0,0,10,0"/>
                <Button Content="❌ Отмена" Width="100" Height="40" Click="CancelButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>